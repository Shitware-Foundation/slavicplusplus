#подключати <stdio.h>
цифра царь_батюшка_главный() {
    строка строченька[1024];
    величина_т величина_строченьки = величина(строченька);
    вычитать_истину(строченька, величина_строченьки, ввод);
    молвить("%s\n", строченька);
    возвращати 0;
}
